# Telegram-бот для управления заявками на 3D-печать

## Описание
Бот для управления очередью заявок на 3D-печать с двумя ролями:
- **Пользователи**: создают заявки с .stl файлами
- **Админ (@sementrepp)**: управляет заявками и их статусами

## Установка и настройка

### 1. Создайте Telegram бота
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен - он понадобится для настройки

### 2. Настройте Google Sheets
1. Создайте новую Google таблицу
2. Скопируйте ID таблицы из URL (между `/d/` и `/edit`):
   ```
   https://docs.google.com/spreadsheets/d/SHEET_ID_HERE/edit
   ```

### 3. Настройте Google Drive
1. Создайте папку в Google Drive для хранения .stl файлов
2. Скопируйте ID папки из URL (после `/folders/`):
   ```
   https://drive.google.com/drive/folders/FOLDER_ID_HERE
   ```

### 4. Получите Service Account ключ
Так как у вас уже настроены интеграции Google Sheets и Google Drive в Replit:
1. Перейдите в Google Cloud Console
2. Создайте Service Account
3. Скачайте JSON ключ
4. Скопируйте весь JSON в переменную окружения

### 5. Настройте переменные окружения (Secrets)
Добавьте следующие секреты в Replit:
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `GOOGLE_SHEET_ID` - ID Google таблицы
- `GOOGLE_DRIVE_FOLDER_ID` - ID папки Google Drive
- `GOOGLE_SERVICE_ACCOUNT_KEY` - JSON ключ Service Account (весь JSON как строка)

## Использование

### Для пользователей
1. Начните диалог с ботом: `/start`
2. Создайте новую заявку: `/new_request`
3. Следуйте инструкциям:
   - Введите имя
   - Введите фамилию
   - Выберите группу из списка
   - Выберите цель печати
   - Прикрепите .stl файл
4. Получите подтверждение с номером очереди
5. Отслеживайте статус заявки (получите уведомления при изменениях)

### Для админа (@sementrepp)
1. Откройте админ-панель: `/admin`
2. Доступные функции:
   - **Просмотр заявок**: список всех заявок с пагинацией
   - **Изменение статуса**: принять в работу / завершить
   - **Управление группами**: добавление/удаление групп
   - **Управление целями**: добавление/удаление целей печати

### Статусы заявок
- ⚪ **В очереди** (белый цвет в Google Sheets)
- 🟡 **В работе** (желтый цвет)
- 🔴 **Готово** (красный цвет)

## Автоматизация
- Файлы автоматически удаляются через 1 неделю после статуса "Готово"
- Еженедельные бэкапы таблицы создаются каждое воскресенье
- Старые бэкапы удаляются через 2 недели

## Структура проекта
```
/
├── main.py                 # Главный файл запуска
├── bot/
│   ├── handlers/
│   │   ├── user.py        # Обработчики для пользователей
│   │   └── admin.py       # Обработчики для админа
│   ├── services/
│   │   ├── sheets.py      # Работа с Google Sheets
│   │   ├── drive.py       # Работа с Google Drive
│   │   └── scheduler.py   # Планировщик задач
│   └── utils/
│       ├── config.py       # Конфигурация
│       └── states.py       # Состояния диалогов
└── README.md
```

## Технологии
- Python 3.11
- python-telegram-bot
- Google Sheets API
- Google Drive API
- APScheduler
